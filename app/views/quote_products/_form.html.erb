<%= simple_form_for quote_product do |form| %>
  <% if quote_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(quote_product.errors.count, "error") %> prohibited this quote_product from being saved:</h2>

      <ul>
        <% quote_product.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.association :item, label_method: :name, value_method: :id, label: 'Product' %>
  
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.number_field :amount %>
  </div>

  <div class="actions">
    <button>Add</button>
  </div>
<% end %>
